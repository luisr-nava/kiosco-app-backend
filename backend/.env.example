# Database Configuration
DATABASE_URL="postgresql://usuario:contraseña@localhost:5432/kiosco_db?schema=public"

# Server Configuration
PORT=3003

# Environment (development, production)
NODE_ENV="development"

# Auth Service URL (External authentication service if needed)
AUTH_SERVICE_URL="http://localhost:3000"

# JWT Secret Key (MUST be at least 32 characters in production)
JWT_SECRET="your-secret-key-here-change-in-production-must-be-at-least-32-chars"

# CORS Configuration
# In development, set to '*' to allow any origin
# In production, set comma-separated list of allowed origins
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"

# Mail configuration (optional)
MAIL_HOST=""
MAIL_PORT=""
MAIL_USER=""
MAIL_PASSWORD=""
MAIL_SECURE=false

# Report delivery
REPORTS_NOTIFICATION_EMAIL=""
REPORTS_FROM_EMAIL=""
